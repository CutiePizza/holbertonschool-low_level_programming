#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i,y;
for (i = 'a'; i <= 'z'; i++)
	putchar(i);
for (y = 'A'; y <= 'Z'; y++)
	putchar(y);
	putchar('\n');
return (0);
}
